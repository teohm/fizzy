# Metrics

Generated by `bin/metrics`. Nothing here is written by hand, and nothing here is
merged into main.

- `metrics.jsonl` — one JSON record per commit, appended forever.
- `index.html` — the same data as a chart. Download it and open it; it is self-contained.

To read the history without cloning:

    git show origin/metrics:metrics.jsonl
